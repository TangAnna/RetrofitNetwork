<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:android.support.design="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/page_rootView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <android.support.design.widget.TabLayout
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/y90"
        android.support.design:tabPaddingStart="500dp"
        app:tabIndicatorColor="@color/colorf46"
        app:tabIndicatorFullWidth="false"
        app:tabIndicatorHeight="@dimen/y4"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/colorf46"
        app:tabTextColor="@color/color999" />

    <View
        android:id="@+id/view_tabBottom"
        android:layout_width="match_parent"
        android:layout_height="@dimen/y16"
        android:background="@color/colorBg" />

    <android.support.v4.view.ViewPager
        android:id="@+id/tab_viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
